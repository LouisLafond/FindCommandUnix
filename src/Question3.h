
#ifndef _FONCTION_H

#include "Pile.h"
int find_by_name(char *dir,char *name,Pile *P);
int begin_by_sign(char *param);

long double convert(char *param);

void find_by_taille(char *dir,char *param,Pile *P) ;

#endif