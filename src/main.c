#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Question1.h"

int main(int argc,char *argv[]) {
    //char *dir = strdup("..");
    //char *n = strdup("Jeu.java");
    //find_all_paths(dir);
    parse_command(argc,argv);
    //int res = find_by_name(dir,n);
    //printf("%d\n",res);
    //free(n);
    //free(dir);
    

    return 0;

}
